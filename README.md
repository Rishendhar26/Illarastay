# IllaraStay

IllaraStay is a Flutter property marketplace for finding and listing PGs, rooms, flats, houses, villas, residential land, and commercial property.

## Run

```bash
flutter pub get
flutter run
```

The v0.1 app uses mock data and an in-memory repository. `PropertyRepository` is the integration boundary for Firebase, Supabase, or a REST API later.
